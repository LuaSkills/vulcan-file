{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "file": {
      "type": "string",
      "description": "Use the exact regular-file path to delete. Supports ${env:NAME} placeholders. Directory paths are rejected."
    },
    "files": {
      "type": "array",
      "description": "Use for batch file deletion. Provide up to 10 items, each with file. Directory paths are rejected for every item. Do not send this field together with root-level file.",
      "minItems": 1,
      "maxItems": 10,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "file": {
            "type": "string",
            "description": "Use the exact regular-file path to delete for this batch item. Supports ${env:NAME} placeholders. Directory paths are rejected."
          }
        },
        "required": [
          "file"
        ]
      }
    },
    "apply": {
      "type": "boolean",
      "description": "Leave false for preview. Set true only when the previewed file deletion is ready to execute. In batch mode this apply flag is shared by all files."
    }
  },
  "oneOf": [
    {
      "required": [
        "file"
      ]
    },
    {
      "required": [
        "files"
      ]
    }
  ]
}
